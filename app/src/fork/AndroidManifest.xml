<?xml version="1.0" encoding="utf-8"?>
<!--
	Fork flavor manifest.

	The manifest merger combines this with app/src/main/AndroidManifest.xml, so anything added here
	extends or overrides upstream without editing upstream's manifest. The provider below merges
	into upstream's androidx.startup InitializationProvider and appends one more initializer.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools">

	<application>
		<provider
			android:name="androidx.startup.InitializationProvider"
			android:authorities="${applicationId}.androidx-startup"
			android:exported="false"
			tools:node="merge">
			<meta-data
				android:name="org.jellyfin.androidtv.fork.startup.ForkInitializer"
				android:value="androidx.startup" />
		</provider>
	</application>
</manifest>
